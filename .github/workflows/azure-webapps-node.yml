name: CI
on:
  push:
    branches:
      - main
jobs:
  setup:
    runs-on: ubuntu-18.04
    steps:
    - run: |
        npm cache clean --force
  build:
    runs-on: ubuntu-18.04
    steps:
    - run: |
        ls
        npm install
        npm run build 
        ls
  deploy:
    runs-on: ubuntu-18.04
    steps:  
    - run: |
        rm -rf public
        mkdir public && cp -rf dist/* public
